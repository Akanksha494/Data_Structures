#include<bits/stdc++.h>

using namesoace std;

int main()
{
	int n;
	cin>>n;
	int a[n];
	for(int i=0;i<n;i++)
	{
		cin>>a[i];
	}
    vector<int>a;
    vector<int>b;
    int maxi=INT_MIN;
    for(int i=0;i<n;i++)
    {
    	if(ar[i]%2==0)
    	{
    		if(ar[i]%5==0)
    		{
    			maxi=max(maxi,ar[i]);
    		    a.push_back(maxi);
    		}
    	}
    }
}